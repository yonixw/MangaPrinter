<Window x:Class="MangaPrinter.WpfGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MangaPrinter.WpfGUI"
        mc:Ignorable="d"
        Title="Manga printer and binder manager" Height="450" Width="800"
        FontSize="9.75pt" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="GeneralStyle" TargetType="{x:Type Control}">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush EndPoint="0.504,1.5" StartPoint="0.504,0.03">
                        <GradientStop Color="#FF2977FF" Offset="0"/>
                        <GradientStop Color="#FF50D8FC" Offset="0.567"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnStyle" TargetType="Button" BasedOn="{StaticResource GeneralStyle}" >
            <Setter Property="Width" Value="150" />
            <Setter Property="Height" Value="35" />
            <Setter Property="Margin" Value="10,10,0,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border" CornerRadius="5" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}"
                                BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center">
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MenuItemBtnStyle" TargetType="MenuItem" BasedOn="{StaticResource GeneralStyle}" >
            <Setter Property="Width" Value="150" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Margin" Value="0,2,0,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border Name="border" CornerRadius="5" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}"
                                BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" ContentSource="Header">
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <DockPanel>
        <TabControl>
            <TabItem Header="Chapter and Files">
                <DockPanel>
                    <Menu DockPanel.Dock="Top" Padding="5px" FontSize="9.75pt">
                        <Label IsEnabled="False">Subfolders?</Label>
                        <CheckBox IsChecked="True"></CheckBox>

                        <Label IsEnabled="False" >Double-Page width (px) cutoff:</Label>
                        <TextBox Name="txtPageMaxWidth" Width="100px" Text="900" TextChanged="txtPageMaxWidth_TextChanged"></TextBox>


                        <Label IsEnabled="False">Direction:</Label>
                        <StackPanel Orientation="Horizontal" >
                            <RadioButton IsChecked="True" Margin="0,0,10,0">RTL (→)</RadioButton>
                            <RadioButton>LTR (←)</RadioButton>
                        </StackPanel>
                    </Menu>
                    <Menu DockPanel.Dock="Top" Padding="5px" FontSize="9.75pt">
                        <MenuItem Header="Import folders" Style="{StaticResource MenuItemBtnStyle}"
                                  Name="menuImprtFolders" Click="menuImprtFolders_Click"/>
                        <MenuItem Header="Add empty chapter" Style="{StaticResource MenuItemBtnStyle}"/>
                        <MenuItem Header="Add files" Style="{StaticResource MenuItemBtnStyle}"/>
                    </Menu>

                    <TreeView>

                    </TreeView>
                </DockPanel>
            </TabItem>
            <TabItem Header="Binding">
                <DockPanel>
                    <Menu DockPanel.Dock="Top" Padding="5px" FontSize="9.75pt">
                        <Label IsEnabled="False">Chapter:</Label>
                        <CheckBox IsChecked="True">Add Start Page</CheckBox>
                        <CheckBox IsChecked="True">Add End Page</CheckBox>

                        <Label IsEnabled="False">Binding:</Label>
                        <StackPanel Orientation="Horizontal" >
                            <RadioButton IsChecked="True" Margin="0,0,10,0">Duplex</RadioButton>
                            <RadioButton>Booklet</RadioButton>
                        </StackPanel>




                    </Menu>
                    <Menu DockPanel.Dock="Top" Padding="5px" FontSize="9.75pt">
                        <MenuItem Header="Bind!" Style="{StaticResource MenuItemBtnStyle}"/>
                    </Menu>
                    <TreeView >

                    </TreeView>
                </DockPanel>
            </TabItem>
            <TabItem Header="Print and Export">
                <DockPanel>
                    <Menu DockPanel.Dock="Top" Padding="0,5">
                        <MenuItem Header="Refresh Preview" Style="{StaticResource MenuItemBtnStyle}"/>
                    </Menu>
                    <Canvas>
                        
                    </Canvas>
                </DockPanel>
            </TabItem>
            <TabItem Header="About">

            </TabItem>

        </TabControl>
    </DockPanel>
</Window>
